import { Metadata } from "next";
import React from "react";

export const metadata: Metadata = {
  title: "About |  Next App",
};

const About = () => {
  return <h3 style={{ textAlign: "center" }}>sub item</h3>;
};

export default About;
